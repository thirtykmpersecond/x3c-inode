# 逆向思路与方法论：从路由器二进制到跨平台 802.1X 客户端

本文记录把某路由器固件里的 `x3c8021x`（H3C/iNode 校园网认证客户端）逆向、
重写并在实网调通的完整思路。重点不是"它用了什么参数"，而是**怎么一步步
找到答案的**——协议细节会因学校而异，方法可以复用。

> 代码见 `reimpl/`，原始二进制在 `bin/`，反汇编在 `disasm/`，同源开源码在
> `src/njit8021xclient/`。

---

## 一、整体路线

```
路由器后台
  │  (1) 免拆机拿 shell
  ▼
拉取二进制 /usr/sbin/x3c8021x
  │  (2) strings → 特征串 → 认出开源项目
  ▼
对照 njit8021xclient 源码，定位"改了什么"
  │  (3) 重写 + 恢复原厂固件命令行
  ▼
跨平台编译（Linux / macOS / Windows+Npcap）
  │  (4) 实网调试
  ▼
抓包对比一个能用的客户端 → 逐字节对齐 → 调通
```

每一步的核心技巧不同，下面分开讲。

---

## 二、第一步：免拆机拿到 shell

该路由器的 SSH 不能直接开，但它的 Web 后台有一个"本地调试"接口，需要提交一个
`cloud_token`。这个 token 的算法是：

```
local_token = base64( "<MAC大写>,ssh,<时间戳>,<签名>" )
msg         = base64_decode(local_token) 取前三段(逗号分隔)，且时间戳 +1
key         = SHA1( uuid )               # uuid 从 /cgi-bin/turbo/proxy/router_info 取
cloud_token = base64( HMAC_SHA1(key, msg) )
```

把 `cloud_token`（注意 `+` 要 URL 编码成 `%2B`）提交给
`/local-ssh/api?method=valid&data=<cloud_token>`，SSH 即开（临时，约 5 分钟）。

**思路要点：**
- token 算法不是猜的，是从固件里的 Web 前端 JS（`webui/`）和网上已有的
  Go 实现（`scripts/main.go`）对照出来的。
- 能在用户态解决的事，不要去拆机刷固件。

参考实现：`scripts/router_open_ssh.py`。

---

## 三、第二步：从二进制认出开源项目（最省力的一步）

拿到 27KB 的 MIPS 二进制后，**先别读汇编**。第一步永远是 `strings`：

```
Oly5D62FaE94W7      ← H3C 新版密钥
HuaWei3COM1X        ← H3C 老版密钥
r70393861           ← Windows 版本区魔术串
EN V3.60-6208       ← 内置版本串
[0] Client: Start.  ← 日志格式
```

这几个串一搜，立刻命中开源项目 **njit8021xclient**（`auth.c` 里的
`H3C_KEY` / `H3C_KEY2` / `WinVersion` 完全一致）。

这一步把工作量从"逆向 6608 条 MIPS 指令"塌缩成**"找出原厂相对上游
改了哪些参数"**。

**思路要点：**
- 特征字符串（密钥、版本号、日志文案）是开源项目的"指纹"。
- 认出同源后，二进制只用于**确认差异**，不必逐行读懂。

用 radare2 确认的差异（见 `disasm/`）：
- 上游是 `njit-client 用户 密码 [网卡]` 三个位置参数；
- 原厂版改成了 getopt_long：`-u -p -I -x -m -i -v -A -P -M -V`；
- 支持 V7 版本格式（分隔符是 `0x11` 而非空格）；
- 内置版本串是 `EN V3.60-6208`（上游是 `CH V3.60-6208`）；
- **没有** IPv6、没有 AES-MD5、没有默认 privikey 串（都是运行时传入）。

---

## 四、第三步：重写

重写不是"翻译汇编"，而是**参照上游源码的协议结构，补上原厂固件的参数体系**。

### 4.1 报文结构

802.1X 跑在二层，EtherType `0x888e`，**完全不经过 IP/TCP**：

```
[以太网头 14B] [802.1X 头] [EAP 报文]
  dst src      ver type len   code id len type data
```

因为是数据报而非字节流，**不存在 TCP 那种粘包问题**——`pcap_next_ex()`
每次返回一个完整帧。代价是没有内建重传，超时重试要自己写。

认证状态机：

```
EAPOL-Start → Request Identity → Response Identity
            → Request <方法>    → Response <方法>
            → Success / Failure
```

"方法"标准是 EAP-MD5（type 4），但 H3C 有私有扩展（见第六节）。

### 4.2 版本区的双重 XOR + Base64

H3C 在 Identity 响应里塞了一个 28 字节的 Base64 区，编码方式：

1. 16 字节版本串（如 `CH\x11V7.xx-yyyy`，不足补零）
2. 用 8 位随机数字符串（`%08x`）正向 XOR
3. 追加 4 字节该随机数（大端）
4. 对全部 20 字节用 H3C 密钥再做**一次正向 + 一次反向** XOR
5. Base64 编码成 28 字节

"反向 XOR"就是从尾到头再异或一遍密钥，逆运算也反过来（先反向再正向）。
这部分代码在 `x3c.c` 的 `XOR()` / `fill_client_version_area()`。

### 4.3 跨平台

最难的不是协议，是**平台兼容层**：

| 功能 | Linux | macOS | Windows |
|---|---|---|---|
| 抓包 | libpcap | libpcap(自带) | Npcap（必须装驱动）|
| 取 MAC/IP | ioctl | getifaddrs | GetAdaptersAddresses |
| 参数解析 | getopt_long | getopt_long | **自己实现**（compat.c）|
| 二层帧 | 内核支持 | BPF | **无内建，必须 Npcap** |

代码用 `#ifdef _WIN32` / `__APPLE__` 分流，平台相关的全在 `dev.c` 和
`compat.c`，协议逻辑（`x3c.c`）平台无关。

---

## 五、第四步（最关键）：实网调试的方法论

重写编译通过 ≠ 能用。真正的突破来自一条原则：

> **不要猜参数。抓一个能正常工作的客户端，逐字节对比。**

### 5.1 加可观测性

调试前先给程序装"仪表盘"。本项目加了四个选项，每一个都直接促成一次定位：

| 选项 | 作用 | 解决的问题 |
|---|---|---|
| `-l` | 枚举网卡与 MAC | 确定 `-I` 填什么 |
| `-L file` | 输出写文件 | 绕开控制台/提权窗口丢输出 |
| `-d` | 十六进制转储**收发**帧 | 看清每个字节 |
| `-S` | **只监听不认证** | 抓第三方客户端的成功报文 |

`-S` 是关键：让程序安静地抓包，然后用 iNode 正常认证一次，就拿到了
"标准答案"。

> 教训：`-d` 一开始只转储了**收到**的帧，没转储**发出**的帧，导致盲了
> 好几轮。排障工具要同时覆盖输入和输出。

### 5.2 对比方法

抓到成功报文后，不要整体看，**按字段拆**：

1. 以太网头：目标 MAC 对不对？
2. 802.1X/EAP 长度字段
3. EAP code / id / type
4. Type-Data 里的每个 TLV
5. 对不上的字段，去上游源码或反汇编里找构造逻辑

### 5.3 用反解代替猜测

版本区是 Base64 且含时间戳随机数，**每次运行都不一样**，直接字符串比对
毫无意义。正确做法是写逆运算，把两边都解成明文版本串再比。

逆运算（`scripts/decode_version.py`）：

```
Base64 解码 (20B)
  → 用 H3C 密钥逆 XOR
  → 后 4 字节大端读出随机数 r
  → 用 "%08x" % r 作密钥逆 XOR 前 16 字节
  → 得到明文版本串
```

用错误的密钥会解出乱码，用正确的密钥解出干净 ASCII——这还能反过来
**验证 `-x` 密钥选择是否正确**。

---

## 六、四个关键发现，以及各自是怎么找到的

这四个差异让程序从"能发包"变成"能认证"。没有一个是靠试参数试出来的。

### 发现 1：EAPOL-Start 的目标 MAC 是私有多播地址

**现象：** 程序发了 Start，服务器毫无反应，只看到交换机周期性广播 Identity。

**怎么找到的：** `-S` 抓包看 iNode 的第一个帧，目标 MAC 是
`01:d0:f8:00:00:03`（H3C 私有多播），而我们和上游开源都发的是标准
`01:80:c2:00:00:03` + 广播。发往标准地址的 Start，这台交换机不受理。

**教训：** 协议的"标准默认值"在厂商私有实现里可能被改。看真实报文的
**第一个包**就能发现。

### 发现 2：认证方法是 type 0x07（明文密码），不是 MD5

**现象：** 响应 Identity 后没有 MD5-Challenge，程序直接退出。

**怎么找到的：** 抓包显示服务器第二阶段请求的 EAP type 字节是 `0x07`，
而代码只处理了 1/2/4/0x14，落到 `default:` 直接 `return -1`。type 0x07
的载荷是 `[密码长度1字节][密码][用户名]`——密码明文传输。

**教训：** 不要假设所有 802.1X 都走 EAP-MD5。先看服务器请求的 type 字节，
再决定实现什么。

### 发现 3：上报版本号不是 WebUI 默认值

**现象：** Identity 结构完全正确，服务器却反复重发同一个请求。

**怎么找到的：** 反解 iNode 的 Base64 版本区，得到一个版本串；和设备
WebUI 里的默认值对比，**不一样**。WebUI 默认值是厂商通用模板，实际
服务器校验的是另一个值。改成反解出的值后立刻通过。

**教训：** 配置界面的默认值 ≠ 服务器实际要求。能从真实流量反解的，
就不要信界面。

### 发现 4：Identity 里不带 IP 属性

**怎么找到的：** 逐字节对比 iNode 的 Identity 响应，Type-Data 里只有
版本区 + 分隔符 + 用户名，没有 `0x15` IP TLV。对应 `-i 0`。

**教训：** TLV 的有无也是协议差异的一部分，别想当然带上所有可选字段。

---

## 七、踩过的坑（值得记住的 bug）

### 7.1 两阶段 API 漏调第二次

Windows `GetAdaptersAddresses` 是"两阶段调用"：第一次传 NULL 探大小，
第二次传分配好的缓冲区填数据。重写时**只写了第一次**，导致在未初始化的
堆内存上遍历链表。

后果极具迷惑性：
- 堆里是垃圾 → 访问违规崩溃（`0xC0000005`）；
- 堆恰好是零 → 链表"为空"，函数返回失败，**但没有任何错误输出**。

后者正是最初"程序零输出"的真凶。凡是 `GetAdaptersAddresses`、
`WinHttpReadData` 这类两阶段 API，第二次调用绝不能漏。

### 7.2 stdout 与 stderr 分开缓冲

调试信息一部分走 `stdout`（`printf`/hexdump），一部分走 `stderr`
（`DPRINTF` 宏）。重定向到同一文件后，两个流各自缓冲，导致：
- 输出**错序**；
- 程序退出时 stderr 末尾内容**滞留缓冲区没落盘**。

这直接造成一次误判：日志显示"服务器没响应"，实际上响应到了，只是没写进
文件。**统一所有日志到一个流**（本项目最终让 DPRINTF 走 stdout）。

### 7.3 读超时设成 60 秒

`pcap_open_live(..., 60000, ...)` 的读超时是 60 秒，而重试循环每次都
调它，于是每次收包最多卡 1 分钟——用户一句"为什么要跑这么久"点破了。
改成 1000ms。

### 7.4 重试计数器收包后不重置

握手循环的超时计数在收到包后没有归零，一直累加，几次空转后就误判超时、
跳回重来。改成局部变量、每次收到包重置。

### 7.5 自写 getopt_long 的标志选项返回 0

Windows 没有 `getopt_long`，自己实现时，对"无参数的短选项"（如 `-l`）
返回了 0，而 POSIX 里 0 表示"选项结束"，导致主循环误以为参数解析完毕、
打印 usage 退出。标志选项必须返回选项字符本身。

---

## 八、安全与伦理说明

- 本项目仅用于**连接你自己有权使用的网络**，以及学习网络协议。
- 使用 type 0x07 的网络中，**密码以明文在二层传输**，任何能在该广播域
  抓包的人都能读到。这是网络侧设计，非客户端缺陷；建议不要在其他系统
  复用同一密码。
- 逆向的是你自己拥有的设备上的固件；请遵守当地法律与软件许可。
- 本仓库已移除所有个人账号、密码、MAC、设备序列号等信息；复现时请
  自行填写，切勿提交真实凭据。

---

## 九、这个项目能学到什么

- **802.1X/EAP 协议**的真实封装与状态机，以及厂商私有扩展长什么样。
- **二层网络编程**：libpcap/Npcap、原始帧构造、网络字节序、无连接下的
  重试设计。对照之下能更理解 TCP 替你做了哪些事。
- **逆向工程的经济思路**：先 strings 找指纹 → 识别同源开源 → 只确认差异，
  而不是硬啃汇编。
- **跨平台 C**：ioctl / getifaddrs / GetAdaptersAddresses 三套 API、
  MinGW 交叉编译、Windows 二层帧必须内核驱动的现实约束。
- **调试方法论**：可观测性优先、抓包对比优于猜参、对含随机数的数据做
  逆运算而非字符串比对。
- **一些会反复出现的 bug 模式**：两阶段 API、多流缓冲、超时与重试计数。

如果只想读一个文件了解协议，看 `reimpl/x3c.c`；想了解怎么定位未知网络的
参数，看本文第五节和 `scripts/decode_version.py`。
